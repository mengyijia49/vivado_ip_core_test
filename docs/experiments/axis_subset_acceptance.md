# 子集转换接入记录

日期：2026-09-15。环境：Vivado 2025.2、Ubuntu，只有行为仿真。

## 常用配置

首批编号 `2026-09-15_01-41-17_UTC+0800_96daeeb5`：
11 组配置的 33 个阶段 PASS，1 组在创建阶段失败。
失败配置关闭了输入 TDATA，却要求从 TUSER 映射输出 TDATA。
工具只允许这种接口使用单个常量映射；这是接入配置问题，不计为 IP bug。

修正配置并增加提前校验后，补跑编号 `2026-09-15_01-47-48_UTC+0800_4150dd27`：
启用输入 TDATA 后的用户字段映射，以及无输入 TDATA 的常量生成，共 6 个阶段 PASS。
当前 13 组常用配置均完成行为自检，旧失败记录没有覆盖。

## 边界配置

首跑编号 `2026-09-15_01-52-55_UTC+0800_92835014`：
包尾周期 255，以及 512 字节数据与 4096 位 TUSER 交换，共 6 个阶段 PASS。
另外两组创建成功，但框架读取 XCI 失败。

原因是 Vivado 把长映射字符串存成 JSON 字符串片段数组，框架此前直接转换整个数组，
误报参数不一致。现按顺序连接片段，再严格比较完整值；没有修改 XCI 或放宽校验。
改变、丢失或调换片段的单元测试仍会失败。这是框架问题，不是 IP bug。

修复后补跑编号 `2026-09-15_01-55-52_UTC+0800_80a1f8ff`，两组的 6 个阶段全部 PASS。
四组边界配置的实际输入拍数如下，含逐位前缀和计数补充拍：

| 配置 | 输入拍数 | 输出包尾数 |
| --- | ---: | ---: |
| 512 字节逐位反转 | 8705 | 1287 |
| 3 字节输入，每 255 拍生成包尾 | 561 | 2 |
| 512 字节数据与 4096 位 TUSER 交换 | 16917 | 2460 |
| 无 TDATA，1 位 TUSER 扩到 4096 位，每 256 拍生成包尾 | 515 | 2 |

同时修正输入顺序记录：`selected_vector_indices` 保留数值样本的打乱顺序，
`transaction_vector_indices` 对应完整输入文件，包含前缀。二者不能混用。

## 公共后端回归

编号 `2026-09-15_01-49-52_UTC+0800_07767ed3`：
寄存切片的完整/旁路模式、异步流 FIFO、时钟转换、3:5 位宽转换，共 15 个阶段 PASS。
这些配置用于检查公共流后端改动是否影响已有 IP，不代表所有扩展参数已经跑过。

Python 检查共 257 项：218 项通过，39 项真实工具测试按默认设置跳过。
下文故障检查单独启用了真实 XSim，不包含在默认跳过项的通过数量里。

默认回归编号 `2026-09-15_01-59-17_UTC+0800_46b74a81`：6 组配置、19 个阶段全部 PASS。
该批归档的 164 个源码文件与当前源码一致，402 个产物哈希检查通过。
本页此前五个真实 IP 批次的归档哈希也全部一致，失败批次一并保留。

## 故障检查

独立替代模块位于 `tests/fixtures/ip/axis_subset_converter/`。
正常映射与包尾计数通过；按时钟而非握手计数、包尾偏一拍、回绕后停止计数、
交换数据字段、TUSER 来源错误，五种故意错误全部被捕获。
六项真实 XSim 检查全部得到预期结果，故意错误不计为真实 IP bug。
产物按时间保存在 `runs/framework/failure_detection/<编号>/axis_subset_converter/`，
日志在对应的 `runs/logs/framework/failure_detection/`。

本次增加 16425 组可选参数，合计 20 类 IP、90 组常用配置、271 个阶段，
大矩阵 77237 组。大矩阵参数与预算检查通过，没有全部运行 Vivado。
