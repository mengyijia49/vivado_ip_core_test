# 广播器接入记录

日期：2026-09-15。环境为 Ubuntu、Vivado 2025.2，只有行为仿真。

## 真实 IP

常用批次 `2026-09-15_02-15-03_UTC+0800_dc4f1c40`：10 组配置、30 个阶段全部 PASS。
覆盖复制、字节轮转、反转、分段拆分、常量、独立 TUSER 映射、无 TDATA 和不同支路数。
16 路配置每条支路接收 801 拍，均实际发生了回压和先于上游完成的握手。

边界批次 `2026-09-15_02-18-59_UTC+0800_d31fa0a8`：3 组配置、9 个阶段全部 PASS。

| 配置 | 输入拍数 | 所有支路接收总数 |
| --- | ---: | ---: |
| 512 字节输入，16 路单字节输出 | 8705 | 139280 |
| 无 TDATA，1 位 TUSER 输入，16 路 4096 位常量输出 | 130 | 2080 |
| 512 字节数据、4096 位 TUSER、32 位 TID/TDEST，2 路输出 | 17025 | 34050 |

拍数包含定向前缀，不等于不同随机数值的数量。
最后一组实际输出文件约 316 MB，原始握手事件另存，没有为了缩小文件而省略支路数据。

## 反例检查

独立替代模块位于 `tests/fixtures/ip/axis_broadcaster/`。
九项真实 XSim 检查全部得到预期结果：正常模块通过，以下八种故意错误被拒绝。

- 支路重复接收。
- 缺少一条支路输出。
- 数据映射错误。
- 回压时数据变化。
- 用户字段映射错误。
- 输出有效信号为未知值。
- 输入尚未提供就输出。
- 提前接收输入且没有保住暂停支路的数据。

产物位于 `runs/framework/failure_detection/<时间编号>/axis_broadcaster/`，
日志位于对应的 `runs/logs/framework/failure_detection/`。
故意错误用于检查框架，不计作真实 IP bug。本次没有确认新的 IP 缺陷。

## 大文件与矩阵

输出行数改为逐行统计，不再整文件读入内存。
无掩码且完全相等的合法二进制行直接比较；不同值、未知值、非法字符和掩码仍严格检查。
已用修改后的文件比较器重新核对三组边界产物，输入和输出全部匹配。

新增 62550 组可选参数，当前合计 21 类 IP、100 组常用配置、301 个阶段，
大矩阵 139787 组。全部矩阵的参数、预算和参数组合去重检查通过，没有全部运行 Vivado。
具体限制见[广播器测试说明](../ip/axis_broadcaster/selfcheck.md)。

## 公共后端检查

批次 `2026-09-15_02-23-02_UTC+0800_96c284a0`：
寄存切片、时钟转换、位宽转换、子集转换、广播器，共 15 个阶段全部 PASS。
采用新的输出统计与比较方式，没有改变参考值或放宽错误判定。

Python 检查共 277 项：229 项通过，48 项真实工具检查按默认设置跳过。
前述九项广播器反例另外启用了真实 XSim，均已执行。
三个真实 IP 批次的归档哈希全部一致，公共回归批次的 173 个源码文件与当前源码一致。

最后运行默认回归，编号 `2026-09-15_02-25-36_UTC+0800_da5b16f2`：
6 组配置、19 个阶段全部 PASS；173 个源码文件和 411 个归档产物的哈希检查通过。
`python3 -m py_compile scripts/run_all.py` 和 `git diff --check` 通过。
