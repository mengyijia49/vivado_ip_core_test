# 浮点转换接入检查

2026-09-15，Vivado 2025.2，Floating-Point Operator 7.1，器件 xc7a35tcsg324-1。
新增绝对值、浮点格式转换、定点转浮点和浮点转定点。只做行为仿真。
参数说明和模型边界见[浮点检查](../ip/floating_point/selfcheck.md)。

## 真实 IP 回归

```bash
source /data/Xilinx/2025.2/Vivado/settings64.sh
python3 scripts/run_all.py --ip-type floating_point
```

编号 `2026-09-15_08-01-57_UTC+0800_8b34d61c`，21 组、63 个阶段全部 PASS。
共核对 416745 次输出，255 份源码和 1096 个归档产物的哈希一致。
期望值不调用厂商 C 模型，也不从 DUT 输出拟合。

四组 16 位检查都包含全部 65536 个输入值：半精度绝对值、半精度转单精度、
16 位定点转单精度、半精度转 4 位定点。
9 位绝对值另遍历了 65536 个总线位型，其中包含全部 512 个数值位型及补齐位变化。
还检查了 80 位浮点、64 位无符号输入、256 位用户侧带，以及异常标志的不同组合。
这些是有限的行为测试，不代表全部参数和时序都通过。

首批编号 `2026-09-15_07-52-17_UTC+0800_caff2173` 有三个下溢边界失败，
原因是参考采用了 PG060 注释的舍入前判断，而实测符合正文的舍入后判断。
该批 255 份源码、1099 个归档产物的哈希一致，未改写历史结果。
当前模型采用正文规则，并记录[手册冲突](../ip/floating_point/underflow_review.md)，
没有把这些差异计作实现 bug。

格式边界另跑了 `configs/ip/floating_point/diagnostics/format_boundaries.json`，
编号 `2026-09-15_08-10-17_UTC+0800_8f696ae4`，4 组、12 个阶段全部 PASS。
检查 4 位纯小数、64 位高小数精度、12 位浮点转整数、64 位无符号转 72 位浮点，
共 75192 次输出。4 位和 12 位数值字段分别包含全部 16、4096 个输入值，另检查补齐位。
255 份源码和 416 个归档产物的哈希一致。

## 检查器与手写对照

```bash
VIVADO_INTEGRATION=1 PYTHONPATH=src:tests python3 -m unittest \
  integration.ip.floating_point.test_failure_detection -fv
```

三项测试已分别运行并通过，共 13 次 XSim，约 211 秒。
正确替身通过，十种人为错误被检出：错误舍入、未知位、回压期间数据变化、
漏发、多发、错误 TLAST、用户侧带、补齐位、溢出位和 NaN 结果。
它们是框架检查，不算 AMD IP bug。

两次真实 IP 对照不使用 Python 参考或自动生成的 testbench：

| 对照 | 输入组数 | 编号 |
| --- | ---: | --- |
| 浮点转定点、饱和及异常标志 | 17 | `2026-09-15_07-58-58_UTC+0800_1da97149` |
| 下溢边界、舍入进位及正负号 | 14 | `2026-09-15_08-01-26_UTC+0800_eec11539` |

手写 VHDL 和参数保存在 `runs/framework/protocol_probe/<编号>/floating_point/`，
日志在对应的 `runs/logs/framework/protocol_probe/`。两组 VHDL 哈希和调用退出码均已复核。

## 原有接口回归

公共流后端只增加了可选时钟和复位端口，原有默认端口不变。
编号 `2026-09-15_08-12-27_UTC+0800_c46b33a7`，寄存片、跨时钟转换器及 CORDIC 的
三组配置、9 个阶段全部 PASS。255 份源码和 383 个归档产物的哈希一致。

三组的输入、期望输出、间隔、READY 和向量记录都与旧批次逐字节一致，
归一化运行路径后的 testbench 也相同。前两组对照旧批次
`2026-09-15_07-31-24_UTC+0800_b0bfa1cd`，CORDIC 对照
`2026-09-15_07-25-22_UTC+0800_a16a315d`。

## 默认流程

`python3 -m py_compile scripts/run_all.py` 通过。
默认 `python3 scripts/run_all.py` 编号 `2026-09-15_08-14-05_UTC+0800_dec5abd9`，
原有六组配置、19 个阶段全部 PASS。255 份源码和 493 个归档产物的哈希一致。
`scripts/run_all.py` 和原有 Divider demo Tcl 没有修改。

## 完整配置检查

新增 1077184 组不同参数，按四种运算分目录，覆盖 13742 组运算与数值格式组合。
工程累计 34 类 IP、245 组常用配置、2493286 组扩展参数。
全量完成配置、去重和插件预算检查，没有全量调用 Vivado。
单元测试共 518 项，362 项通过、156 项真实工具测试按规则跳过，耗时约 1136 秒。
真实工具检查的执行结果已单独列在上文，不能用跳过结果代替实测。
