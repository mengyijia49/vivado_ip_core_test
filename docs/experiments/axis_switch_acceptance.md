# 流交换器接入检查

2026-09-15，Vivado 2025.2，`axis_switch:1.1` 修订 35，Artix-7。
只运行创建、testbench 生成和行为自检，没有综合、实现或网表仿真。

## 常用回归

编号 `2026-09-15_03-30-22_UTC+0800_185d0038`，10 组配置、30 个阶段全部 PASS。

| 配置 | 输入组数 | 收发各自的传输数 |
| --- | ---: | ---: |
| switch_route1to3 | 586 | 586 |
| switch_arb3to1 | 550 | 1650 |
| switch_cross3to3 | 718 | 2154 |
| switch_packet5to2 | 1224 | 6120 |
| switch_quota7to1 | 752 | 5264 |
| switch_idle4to3 | 756 | 3024 |
| switch_route1to16_dest32 | 660 | 660 |
| switch_cross16to16 | 642 | 10272 |
| switch_side3to2 | 532 | 1596 |
| switch_id2to1 | 26 | 52 |

包含单输入路由、单输出仲裁、多进多出、三种仲裁算法、包尾/次数/空闲周期释放，
以及无 DATA、32 位 TDEST、反序和带空洞路由。
192 份当前源码与归档记录一致，1431 份归档文件哈希核对通过。

## 边界配置

编号 `2026-09-15_03-33-58_UTC+0800_944fecd1`，另外 4 组配置、12 个阶段全部 PASS。

| 配置要点 | 输入组数 | 收发各自的传输数 |
| --- | ---: | ---: |
| 16 进 1 出，仲裁配额 1024 | 7168 | 114688 |
| 1 位 USER，1 进 8 出，反序路由 | 56 | 56 |
| 4096 位 USER，3 进 5 出 | 25590 | 76770 |
| 511 字节数据、完整侧带，3 进 5 出 | 25932 | 77796 |

小数值空间配置的 8 个输出各收到 7 笔，确认路由补齐实际执行。
两组宽接口的实际输出分别为 315064080 和 403294464 字节，没有截断后再比较。
本批 192 份源码和 557 份归档文件哈希核对通过。

## 框架报错检查

```bash
source /data/Xilinx/2025.2/Vivado/settings64.sh
VIVADO_INTEGRATION=1 PYTHONPATH=src:tests python3 -m unittest integration.ip.axis_switch.test_failure_detection -v
```

14 项真实 XSim 检查通过：1 个正常对照和 13 个故障对照。
正常对照刻意让同一来源发往不同输出的后一个数据先完成，检查器正确接受。
故障包含错误路由、数据/USER/ID/LAST/来源标记错误、漏发、重发、未知 VALID/READY、
回压时数据变化、合法输入报译码错，以及没有输入握手就输出。
这些是检查器测试，不是发现了 13 个 IP bug。

测试电路在 `tests/fixtures/ip/axis_switch/`，期望值独立写在测试中，
没有调用插件参考模型来制造预期结果。
证据保存在 `runs/framework/failure_detection/<时间编号>/axis_switch/` 和对应日志目录。

## 接入期间修正

首次回归 `2026-09-15_03-12-37_UTC+0800_db51f871` 有 3 组端口校验失败：
单输出仲裁配置多出 `s_req_suppress`。后端已按实际接口补上，当前固定为 0。
这是框架接线遗漏，不算 IP bug；旧日志保留。

另补上小数值空间的路由定向序列，避免预算只有 2 时只访问少数输出。
最大仲裁配额会相应增加输入组数，报告单独说明数值预算以外的定向输入。

新增矩阵为 93972 组，当前合计 23 类 IP、120 组常用配置、361 个阶段，
大矩阵合计 270919 组。全部参数、预算和重复组合检查通过，没有全跑大矩阵。
Python 检查为 327 项：253 项通过，74 项真实工具检查默认跳过。
默认回归 `2026-09-15_03-37-36_UTC+0800_d2f3fdf0` 的 6 组配置、19 个阶段全部 PASS，
状态为 completed、NO_FAILURE_OBSERVED；192 份源码和 430 份归档文件哈希一致。
本轮尚未发现可以确认的 Switch IP bug。范围和限制见[自检说明](../ip/axis_switch/selfcheck.md)。
