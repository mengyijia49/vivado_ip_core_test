# 流接口接入记录

日期：2026-09-14。工具：Vivado 2025.2，行为仿真。没有综合或实现步骤。

## 代表配置

增加寄存器片、数据 FIFO、时钟转换器三类，共 9 组常用配置：

- 寄存器片四种模式：Default、Fully_Registered、Light_Weight、Bypass。
- FIFO：同步普通、同步包、异步包模式。
- 时钟转换器：8 位、64 位，异步同步级数为 2、4。

首次运行编号：`2026-09-14_22-35-06_UTC+0800_a2a99447`。
7 组完成自检；2 组同步 FIFO 在参数核对阶段失败。
原因是 Vivado 把同步模式中无用的 SYNCHRONIZATION_STAGES 固定为 3，配置却写了 2。
这是接入配置问题，不是 IP bug。修正约束后，只补跑这两组：
`2026-09-14_22-37-48_UTC+0800_909fed81`，6 个阶段全部 PASS。

两批合计 9 组均完成创建和自检，检查 5632 笔输入及对应输出。
报告在各自的 `reports/history/<编号>/`，没有改写首次失败记录。

每组均记录了非零输入暂停和输出暂停次数，不是一直把 ready 拉高。
例如异步 FIFO 的 1024 笔收发一致，输入暂停 505 周期、输出暂停 1228 周期。
这些是观察到的事件数量，不是状态覆盖率。

## 配置规模与限制

大矩阵增加 5424 组参数，合计 12 类、10916 组；常用回归为 29 组、88 个阶段。
大矩阵只做 Python 配置和预算检查，没有全部创建或仿真。
框架故障注入检查另见 `tests/integration/test_stream_failure_detection.py`。

## 框架检查

`python3 -m py_compile scripts/run_all.py` 通过。
常规测试共 151 项，142 项通过，9 项真实工具测试按默认设置跳过。
原有六组回归的 19 个阶段全部 PASS，编号：
`2026-09-14_22-42-44_UTC+0800_d9ad7590`。

真实 XSim 故障检查使用正常对照和五种故意错误：未知值、回压不稳定、
多余输出、丢失输出、数据错误。首次编号为 `2026-09-14_22-40-23_UTC+0800_d337a1a7`；
回压不稳定的替代模块有 VHDL 位宽表达式错误，修正后单独补跑，编号为
`2026-09-14_22-44-29_UTC+0800_c4d6a251`。最终六种场景均得到预期判定。
这些替代模块不是真实 IP，故意错误不计作发现的 bug。

部分有效字节、运行中复位、包模式最早释放条件和更多时钟关系还没覆盖。
不把当前 PASS 或故意注入的错误算成真实 IP bug。
共用检查规则见 [AXI-Stream 自检](../protocols/axis_stream.md)。
