# 汇合器接入记录

日期：2026-09-15。Ubuntu、Vivado 2025.2，只运行行为仿真。

## 真实 IP

批次 `2026-09-15_02-44-11_UTC+0800_6bdc7b0f`：10 组配置、30 个阶段全部 PASS。

| 配置 | 输入组数 / 输出拍数 | 各路输入握手总数 |
| --- | ---: | ---: |
| 两路 8 位 | 291 | 582 |
| 三路完整侧带，主接口 2 | 762 | 2286 |
| 三路完整侧带，主接口 0 | 762 | 2286 |
| 五路 7 字节与侧带 | 1840 | 9200 |
| 十六路 32 字节 | 9265 | 148240 |
| 四路纯侧带 | 653 | 2612 |
| 三路无包尾 | 930 | 2790 |
| 七路仅 ID | 590 | 4130 |
| 四路 129 位 USER | 1549 | 6196 |
| 两路 1 位 USER 数值穷举 | 11 | 22 |

组数包含逐位前缀，不等于随机预算。最后一项有 4 种数值组合，加上前缀共 11 组，
不是穷举了全部协议时序。各配置都实际发生了部分输入先到和输出回压。

边界批次 `2026-09-15_02-48-46_UTC+0800_469f9710`：2 组配置、6 个阶段全部 PASS。

- 三路每路 170 字节、129 位 USER、KEEP/STRB：9962 组，29886 次输入握手。
- 四路每路 4096 位 USER：33797 组，135188 次输入握手。

后一项实际输出含 16384 位 USER 和 1 位 TLAST，文件约 554 MB。
报告以十六进制精确保存输入空间大小，并标注输入比例发生浮点下溢。
两批分别核对了 603、267 个归档文件，均与记录的哈希一致；182 个源码文件也一致。

## 检查器反例

```bash
VIVADO_INTEGRATION=1 PYTHONPATH=src:tests python3 -m unittest integration.ip.axis_combiner.test_failure_detection -v
```

12 项真实 XSim 测试均得到预期结果，用时约 182 秒。
正常模块通过；其余 11 项分别检测：输入未到齐便发 VALID、数据路交换、
包尾/ID/DEST 主接口选错、USER 路交换、回压时变化、未知 VALID、漏发、
缺少某路输入握手、未知 READY。

替代电路位于 `tests/fixtures/ip/axis_combiner/`，
每次测试保存到 `runs/framework/failure_detection/<时间编号>/axis_combiner/`。
这些是人为故障，不是找到的厂商 IP bug。本次没有确认新的 IP 缺陷。

## 配置和 Python

新增 37160 组不同参数，当前共 22 类 IP、110 组常用配置、331 个阶段，
可选大矩阵 176947 组。全部参数、预算和重复组合检查通过，没有全跑大矩阵。
普通测试共 301 项：241 项通过，60 项真实工具检查默认跳过。
上面的 12 项汇合器检查另行启用了真实工具，确实已经执行。

单输入流接口仍保存一列间隔；多输入接口才保存各路间隔矩阵。
未改写旧运行产物，也没有放宽数值或握手判定。

## 公共回归

批次 `2026-09-15_02-51-52_UTC+0800_43face60`：寄存切片、时钟转换、
子集转换、广播器和汇合器，共 15 个阶段全部 PASS。
182 个源码文件和 398 个归档文件的哈希检查通过。

默认批次 `2026-09-15_02-54-11_UTC+0800_8abace79`：原有 6 组配置、19 个阶段全部 PASS，
包括 Divider 官方 demo。182 个源码文件和 420 个归档文件的哈希检查通过。
`python3 -m py_compile scripts/run_all.py`、`git diff --check` 和新增文件空白检查均通过。
