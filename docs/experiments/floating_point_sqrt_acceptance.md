# 浮点平方根接入检查

2026-09-15，Vivado 2025.2，Floating-Point Operator 7.1，器件 xc7a35tcsg324-1。
只做行为仿真。参数和限制见[浮点检查](../ip/floating_point/selfcheck.md)。

## 真实 IP

```bash
source /data/Xilinx/2025.2/Vivado/settings64.sh
python3 scripts/run_all.py --config configs/ip/floating_point/regression/square_root.json
```

编号 `2026-09-15_08-35-46_UTC+0800_a53186c6`，12 组、36 个阶段全部 PASS。
共核对 289639 次输出，256 份源码和 737 个归档产物的哈希一致。
未发现新的 IP 异常，没有放宽数值比较或改写旧记录。

8 位配置包含全部 256 个输入值；两组半精度配置各包含全部 65536 个输入值，
分别使用每周期一次和每 12 周期一次的运算设置。
9 位数值另遍历了全部 65536 个总线位型，覆盖全部 512 个数值位型及补齐位变化。
还检查了 64/80 位、最高每 65 周期一次、256 位用户侧带和 INVALID_OP 开关。
每组都实际发生输入等待和输出回压，不只是生成了时序配置。

这些结果不代表所有参数、时序和内部状态都通过，也不是固定延迟或吞吐率性能验收。
启动后暂不插入运行中复位，ACLKEN、NonBlocking 和手动延迟尚未接入。

## 参考和独立对照

Python 只使用整数移位、整数平方根及精确的舍入中点平方比较。
单元测试用小格式有理数表穷举、全部半精度正正规数的宿主平方根，
以及宽格式的有理数中点区间交叉检查，不调用厂商模型。
[PG060](https://docs.amd.com/v/u/en-US/pg060-floating-point) 的特殊值规则单独检查。

```bash
VIVADO_INTEGRATION=1 PYTHONPATH=src:tests python3 -m unittest \
  integration.ip.floating_point.test_failure_detection -fv
```

四项测试通过，实际启动 15 次 XSim，耗时约 261 秒。
包括正确替身、十种人为错误、原有定点转换和下溢对照，以及两次平方根对照。
人为错误都被检出，它们不是 AMD IP bug。

平方根手写 VHDL 直接列出 22 组输入和期望值，不调用 Python 数值参考：

| 运算间隔 | 编号 |
| --- | --- |
| 1 周期 | `2026-09-15_08-41-03_UTC+0800_5e56adcc` |
| 12 周期 | `2026-09-15_08-41-24_UTC+0800_707a05a5` |

两次均通过，覆盖特殊值、完全平方、非整数平方根和舍入边界，并检查回压保持。
VHDL、参数及日志在 `runs/framework/protocol_probe/` 和对应日志目录下按编号保存。
两份 VHDL 哈希和工具退出码已复核。

## 参数矩阵

新增 1390016 组不同参数，覆盖 645 种浮点格式及各自全部合法运算间隔。
矩阵按指数、精度分目录，单个文件最多 4160 组，不全量调用 Vivado。
运算间隔的合法边界和拒绝记录见[Catalog 探测](catalog_candidates.md)。

流接口预算校验新增上限为 512 项的计数缓存，只保存定向数量和总输入空间大小。
随机输入、定向向量、覆盖标签和生成器状态没有缓存或改写。
同一 80 位数据、256 位侧带的端口结构重复检查一万次，本机测得未缓存约 15.84 秒、
缓存约 0.004 秒；这是局部计数测试，不代表整批仿真能加速同样倍数。

完整单元测试共 531 项，374 项通过、157 项真实工具测试按规则跳过，耗时约 586 秒。
全量检查了 3883302 组参数的去重、格式和插件预算；没有抽样替代这项检查。
工程仍为 34 类 IP，常用回归增至 257 组、772 个阶段。

## 原有接口回归

编号 `2026-09-15_08-43-48_UTC+0800_5c4b01c7`，8 组、24 个阶段全部 PASS。
检查寄存片、跨时钟转换器、字节位宽转换器、CORDIC，以及浮点绝对值和三种转换。
256 份源码和 591 个归档产物的哈希一致。

每组输入、期望输出、间隔、READY、向量记录五个文件均与旧批次逐字节一致，
归一化运行路径后的 testbench 也相同。对照批次分别为：

- 普通流接口和 CORDIC：`2026-09-15_08-12-27_UTC+0800_c46b33a7`。
- 字节位宽转换：`2026-09-15_02-23-02_UTC+0800_96c284a0`。
- 浮点原有四种运算：`2026-09-15_08-01-57_UTC+0800_8b34d61c`。

## 默认流程

`python3 -m py_compile scripts/run_all.py` 通过。
默认 `python3 scripts/run_all.py` 编号 `2026-09-15_08-47-01_UTC+0800_92a1c329`，
原有六组配置、19 个阶段全部 PASS，256 份源码和 494 个归档产物的哈希一致。
`scripts/run_all.py` 和原有 Divider demo Tcl 没有修改。
