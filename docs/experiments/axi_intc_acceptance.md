# AXI INTC 接入检查

2026-09-15，Vivado 2025.2，AXI INTC 4.1 修订 22。只运行行为仿真。
新增 12 组常用配置、8960 组扩展参数，扩展矩阵未全部运行。
功能范围和限制见[自检说明](../ip/axi_intc/selfcheck.md)。

## 常用配置

```bash
source /data/Xilinx/2025.2/Vivado/settings64.sh
python3 scripts/run_all.py --ip-type axi_intc
```

编号 `2026-09-15_11-42-32_UTC+0800_8831ca8d`：
12 个 create_ip 和 12 个 generate_testbench 均 PASS，12 个 sim_selfcheck 均为 SIMULATION_FAILED。
每个仿真都完成全部输入，不是编译、超时或空日志造成的失败。

本批比较 125012 条操作观察，原始数值样本为每组 256 个。
19267 条差异主要重复触发下述 ISR 和 ME 现象，不能称为 19267 个 bug。
全部输出重新按期望和掩码逐行检查，12 份输入文件与握手接受记录一致。
读数差异只出现在 ISR、IPR、IVR；IRQ 也有差异。总线响应、IER、MER、
已测试的 ILR 阈值读数，以及独立的优先级、原子使能和复位定向组没有差异。
随机序列仍需进一步缩减，不能仅凭字段相同就宣称已经完成根因去重。

逐阶段汇总编号 `2026-09-15_11-49-15_UTC+0800_9a39ee92`，位于
`reports/framework/intc_matrix_analysis/`，包含各配置的差异字段、首条操作和输入输出哈希。
275 份运行源码及 792 个归档产物的哈希已复核，归档源码与当前运行源码一致。

## 大预算运行

```bash
python3 scripts/run_all.py --ip-type axi_intc --case intc_mixed32 \
  --case intc_mixed4_lowirq --budget 4096 --seed 20260915
```

编号 `2026-09-15_11-48-25_UTC+0800_11c97d7a`，两组创建和 testbench 生成通过，
两组功能比较失败，均完成所有输入，没有超时。
32 路配置比较 104029 条操作，4 路低有效 IRQ 配置比较 158009 条，合计 262038 条。
新增随机输入仍在 IRQ 和 ISR/IPR/IVR 出现差异，没有其他寄存器或响应字段差异。
全部输入接受记录一致，275 份源码及 362 个归档产物哈希已复核。

## 独立复现

[ISR 写入问题](../ip/axi_intc/isr_write_issue.md)在没有外部输入活动的 VHDL 中复现。
接入过程中又发现[ME 清零后 IRQ 保持](../ip/axi_intc/master_enable_issue.md)，
其 VHDL 只触发硬件输入、不写 ISR，预编译库与直接编译原始 HDL 都能复现。
后者 13 项观察中有 11 项正向对照通过，两条差异是同一个现象。
两项都仍待厂商确认，不将多个位宽、种子或输出极性算成独立缺陷。

整理公共复现辅助代码后，再运行四次独立检查，耗时约 82 秒，结果保持一致：

| 项目 | 原始 HDL 路径 | 预编译库路径 |
| --- | --- | --- |
| ISR | `2026-09-15_11-53-50_UTC+0800_b4122164` | `2026-09-15_11-54-13_UTC+0800_77de574f` |
| ME | `2026-09-15_11-54-49_UTC+0800_a3a04739` | `2026-09-15_11-54-31_UTC+0800_95d51d4c` |

四次仍返回规范检查失败，不标为通过或跳过。每次 testbench、XCI 和八份依赖文件哈希已复核；
依赖记录含复现的 Python 辅助代码和调用文件。ISR 每次 14 项观察，ME 每次 13 项观察。

## 接入修正

首轮 `2026-09-15_11-36-18_UTC+0800_6535aa07` 的两组 IP 创建成功，
但元数据检查错误地要求 HDL 模型含有界面参数 C_IRQ_CONNECTION。
现仍核对其组件配置，只不再要求不存在的模型参数；这是框架接入错误。

次轮 `2026-09-15_11-37-13_UTC+0800_79110e7d` 已完成仿真。
除 ISR/ME 外，还观察到 ILR 大整数写入后的高位读回与整字存储假设不符。
手册将其描述为优先级序号，字段图与全字读写表述尚需厘清。
原记录保留，目前只对 0 至中断总数及全一值建立严格预期，不把其他高位编码计作确认缺陷。
没有把原始 HDL 的截取规则照抄进 Python 模型来消除差异。

## 仓库检查

编号 `2026-09-15_11-43-28_UTC+0800_fed9dbc1`：`py_compile` 通过。
完整测试 600 项，429 项通过，171 项真实集成测试按环境开关跳过，耗时约 815 秒。
上述 INTC 独立复现另外运行，仍失败，不能用单元测试通过代替 IP 功能结论。
峰值内存约 6.53 GiB；大矩阵完整校验目前仍较耗时、耗内存。
检查前后的 275 份运行源码、212 份测试文件和 1325 份配置文件哈希一致。
输入清单、日志和结果位于对应的 `framework/intc_matrix_validation/<编号>/` 目录。

默认命令 `python3 scripts/run_all.py` 的编号为
`2026-09-15_11-55-30_UTC+0800_a38323d4`，原有六组配置、19 个阶段全部 PASS，含 Divider 官方 demo。
275 份源码和 513 个归档产物的哈希已复核；入口及原有仿真 Tcl 没有修改。
`reports/latest` 指向这批默认回归，INTC 的失败记录仍在各自的历史编号目录中。
