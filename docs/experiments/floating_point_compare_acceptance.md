# 浮点比较接入检查

2026-09-15，Vivado 2025.2，Floating-Point Operator 7.1，器件 xc7a35tcsg324-1。
只做行为仿真，范围和命令见[比较器检查](../ip/floating_point/compare.md)。

## 真实 IP

17 组配置分两批完成，共 51 个阶段、1273186 次输出，全部 PASS。

| 批次 | 配置数 | 阶段数 | 源码数 | 归档产物数 |
| --- | ---: | ---: | ---: | ---: |
| `2026-09-15_09-01-54_UTC+0800_d2217508` | 3 | 9 | 263 | 384 |
| `2026-09-15_09-10-32_UTC+0800_b29bce20` | 14 | 42 | 263 | 824 |

两批源码和产物的哈希均已复核。第二批通过 `--resume-from` 跳过前三组，未重复计数。
这次未发现新的 IP 异常，也未放宽输出比较或改写旧记录。

8 位格式的八种固定模式各覆盖全部 65536 对输入，另有 534 组定向前缀。
8 位可编程配置对全部输入对执行七种操作，加上前缀后共 462490 次输出。
其他配置检查非整字节输入、最高 80 位数值、768 位用户侧带、包尾选择及 Or/And。
每组均实际出现独立输入间隔、输入等待和输出回压。
这些配置测得各路累计接收数最大差为 1；该计数不能用来推断内部队列深度。

## 独立对照与人为故障

```bash
source /data/Xilinx/2025.2/Vivado/settings64.sh
VIVADO_INTEGRATION=1 PYTHONPATH=src:tests python3 -m unittest \
  integration.ip.floating_point.test_compare_failure_detection -fv
```

两项测试通过，实际启动 15 次 XSim，耗时约 259 秒。

手写 VHDL 对照编号 `2026-09-15_09-16-50_UTC+0800_5beb882e`。
它直接列出 12 组输入和期望值，独立驱动三路输入，不调用 Python 数值参考或生成模板。
覆盖正负数、正负零、次正规数、无穷、NaN、操作码补齐位、侧带和输出回压。
12 组均通过，VHDL 副本的哈希和工具退出码已复核。

故障替身是测试代码，不是 AMD IP。正常替身有三路独立的四项队列，
在 `2026-09-15_09-17-12_UTC+0800_894a1f98` 中通过，测得输入领先差为 4。
因此驱动没有把每组输入之间强行串行化。
其余 13 种替身分别制造数值、用户字段、包尾、未知值、回压时数据变化、
漏发、多发、过早输出、撤回 VALID、未知 READY、错误操作码解读、错误补齐位和丢失操作数。
它们均被检查器检出，最后一项编号为 `2026-09-15_09-20-51_UTC+0800_77a2276e`。
各次运行保存自己的替身副本、testbench、参数和哈希，日志按同一编号分目录保存。

首次“撤回 VALID”替身只在 READY 为低时隐藏 VALID，没有先产生必须保持的有效输出，
因而未构成预期违规。失败记录 `2026-09-15_09-11-55_UTC+0800_9300ba23` 继续保留。
修正替身后，它先给出有效输出，再在等待期间撤回；
`2026-09-15_09-19-41_UTC+0800_f378c534` 被正确检出。
这是人工测试替身的问题，不是漏掉了一个已存在的 AMD IP bug。

## 原有功能

`2026-09-15_09-18-30_UTC+0800_772810bb` 检查绝对值、三种转换和低速 80 位平方根，
5 组、15 个阶段全部 PASS，263 份源码和 464 个归档产物的哈希一致。
每组输入、期望输出、间隔、READY、向量记录五个文件及归一化路径后的 testbench
均与之前批次一致。前四种运算对照 `2026-09-15_08-43-48_UTC+0800_5c4b01c7`，
平方根对照 `2026-09-15_08-35-46_UTC+0800_a53186c6`。

## 参数和单元测试

新增 1191960 组不同参数：固定模式 866880 组，可编程模式 325080 组。
共 77 个叶文件，最大文件 19656 组，覆盖全部 645 种合法浮点格式。
17 组常用配置的参数均能在大矩阵中找到。
比较模式、独立侧带和包尾规则有专属参数 schema，不与单输入转换格式混用。

完整单元测试共 541 项，382 项通过、159 项真实工具测试按规则跳过，耗时约 711 秒。
其中完整检查了 5075262 组参数的去重、格式和插件预算，没有以抽样替代。
工程仍为 34 类 IP；常用回归为 274 组、823 个阶段。
大矩阵加载期间观察到进程常驻内存超过 10 GB，后续需要改进分批加载和校验。
这些矩阵检查没有调用 Vivado，不能据此认为全部参数都已通过真实仿真。

## 默认流程和打包

`python3 -m py_compile scripts/run_all.py` 通过。
默认 `python3 scripts/run_all.py` 编号 `2026-09-15_09-22-40_UTC+0800_fd3fe4e2`，
原有六组配置、19 个阶段全部 PASS，263 份源码和 501 个归档产物的哈希一致。
`scripts/run_all.py` 和原有 Divider demo Tcl 没有修改。

比较模板已加入 Python 包资源清单。
本机系统 Python 没有 pip 和构建工具，首次打包检查在启动前失败，未改动系统环境。
随后在 `runs/framework/package_check/2026-09-15_09-23-31_UTC+0800_0f3bb46d/`
中隔离下载构建依赖，核对 PyPI 的 SHA256 后构建 wheel。
使用 setuptools 75.8.0、wheel 0.45.1、packaging 24.2，版本和下载哈希保存在该目录。
wheel 内模板与源码一致，从独立目录导入也通过；构建副本和日志按时间保留。
