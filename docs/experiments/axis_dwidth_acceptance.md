# 位宽转换接入记录

日期：2026-09-15。环境：Vivado 2025.2、Ubuntu，测试范围仅行为仿真。

## 首批结果

编号 `2026-09-15_01-09-39_UTC+0800_711d2da4`。
8 组常用配置的创建、testbench 生成、行为自检共 24 个阶段全部 PASS。
包括上下转换、非整数比例、同宽和无包尾连续流。
这批之后补充了显式 TKEEP=0/TSTRB=1 检查，后续结果单独记录，不覆盖旧文件。

联合回归编号 `2026-09-15_01-18-36_UTC+0800_9f0dccbe`：
8 组位宽转换和原有 9 组透明流配置，51 个阶段全部 PASS。
其中位宽转换共比较 36154 个字节/包尾条目。

## 边界配置

随后把期望条目改为逐条计算和写入，避免宽总线展开后占用大量内存。
重新计算的 8 组常用配置期望值、比较掩码和输入对应关系，共 24 个文件，
与上批已仿真的文件哈希完全一致。

补测编号 `2026-09-15_01-27-00_UTC+0800_72ad3ef2`，12 个阶段全部 PASS：

| 配置 | 输入拍 | 输出拍 | 比较条目 |
| --- | ---: | ---: | ---: |
| 512:1，无侧带 | 1030 | 527360 | 527360 |
| 1:512，部分字节和包尾 | 3846 | 15 | 3803 |
| 3:7，无包尾，切换 ID/DEST | 1080 | 849 | 3240 |
| 2:1，每字节 TUSER 2048 位 | 1037 | 1455 | 1561 |

每组都有实际输入暂停和输出暂停。输入与输出拍数不必相同，比较条目也不是输出拍数。
报告在 `reports/history/<编号>/`，原始输出和日志随同归档；这三批归档哈希均已核对。

## 检查器自身

真实 XSim 故障检查使用一份独立替代模块，不改 Vivado 生成文件。
正常对照保留空字节变化和位置字节未知值，应当通过。
另外 9 项分别注入数据错误、丢包尾、错 TUSER、保留字节组合、回压不稳定、
多余包、未接收输入就输出、有效数据未知和丢位置字节，均得到预期失败。

该批时间为 01:18:35 至 01:21:01，文件按时间编号保存在
`runs/framework/failure_detection/<编号>/axis_dwidth_converter/`，日志在对应 `runs/logs/`。
内存调整后，于 01:27:02、01:27:18 再跑正常对照和错 TUSER，结果不变。
原有透明流的 6 项真实 XSim 故障检查也全部通过。
这些故意错误只用于测试检查器，不计作发现的 IP bug。

常规测试 237 项：204 项通过，33 项真实工具测试按默认设置跳过。
`python3 -m py_compile scripts/run_all.py`、新旧文件空白检查均通过。
最终默认回归编号 `2026-09-15_01-29-15_UTC+0800_c40cddf2`，原有 19 个阶段全部 PASS。
该批 158 份源码与当前文件一致，396 项归档文件哈希均正确。

本次增加 5053 组可选参数。合计 19 类 IP、77 组常用配置、232 个报告阶段，
大矩阵共 60812 组。只做代表性仿真，没有运行完整大矩阵。

检查方法和限制见[位宽转换自检](../ip/axis_dwidth_converter/selfcheck.md)。
