# 切片 IP 接口探测

2026-09-15，Vivado 2025.2，`xlslice:1.0` 修订 5。
编号 `2026-09-15_04-24-24_UTC+0800_9e28bec5`，两组创建成功，当时尚未接入功能自检。

`upper255` 的输入为 4096 位，取 255 至 247 位，输出为 9 位。
`crossbyte` 的输入为 33 位，取 32 至 7 位，输出为 26 位。
端口名区分大小写，为输入 `Din` 和输出 `Dout`，没有时钟、复位或握手。

本机 Catalog 声明 DIN_WIDTH 为 2 至 4096，但 DIN_FROM、DIN_TO 的范围仍为 0 至 255。
不要因输入能到 4096 位就直接把切片位置扩到 4095。
后续已按此范围接入[数值自检](selfcheck.md)，输出宽度由两端位置确定。

参数、XCI 和模型保存在 `runs/framework/catalog/<编号>/xlslice/<配置>/`，
命令与日志在对应 `runs/logs/framework/catalog/`。没有把创建成功当作功能通过。
