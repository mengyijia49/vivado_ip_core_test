# 常量 IP 接口探测

2026-09-15，Vivado 2025.2，`xlconstant:1.1` 修订 10。
编号 `2026-09-15_04-24-24_UTC+0800_9e28bec5`，两组创建成功，当时尚未接入功能自检。

`wide4096_one` 为 4096 位常量 1；`wide128_high` 为 128 位常量 `2**127+1`。
只有向量输出 `dout`，没有输入、时钟、复位或握手。
本机 Catalog 宽度范围为 1 至 4096。XCI 把宽常量保存为十六进制，
4096 位值还会拆成多个字符串片段，解析时必须保留全部片段和位宽。

后续已接入[无输入常量自检](selfcheck.md)，没有编造输入端口或用重复输入凑数量。
检查高位常量、零扩展和多时刻输出保持。
XCI 参数一致只说明参数进入模型，不能代替读取实际仿真输出。

参数、XCI 和模型保存在 `runs/framework/catalog/<编号>/xlconstant/<配置>/`，
命令与日志在对应 `runs/logs/framework/catalog/`。
