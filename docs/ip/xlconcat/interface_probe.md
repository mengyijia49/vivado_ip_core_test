# 拼接 IP 接口探测

2026-09-15，Vivado 2025.2，`xlconcat:2.1` 修订 7。
编号 `2026-09-15_04-24-24_UTC+0800_9e28bec5`，两组创建成功，当时尚未接入功能自检。

`mixed3` 的三个输入为 1、7、33 位，输出 41 位。
`ports128` 有 128 个输入，首尾各 4096 位，其余各 1 位，输出 8318 位。
端口是 `In0` 至所选末路和 `dout`，没有时钟、复位或握手，1 位输入仍为向量。

本机 Catalog 的端口数范围为 1 至 128，每路位宽为 1 至 4096。
后续已接入[数值自检](selfcheck.md)，各路宽度保存在参数中，不从用例名称推断。

参数、XCI 和模型保存在 `runs/framework/catalog/<编号>/xlconcat/<配置>/`，
命令与日志在对应 `runs/logs/framework/catalog/`。本次只确认创建和接口，不计入支持数量。
